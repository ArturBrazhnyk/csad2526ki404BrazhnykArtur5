user: у мене є два файли math_operations.h та math_operations.cpp у   math_operations.h створіи функцію intadd(int a, int b); а інший заповни сам 
copilot:
#ifndef MATH_OPERATIONS_H
#define MATH_OPERATIONS_H

int add(int a, int b);

#endif // MATH_OPERATIONS_H

#include "math_operations.h"

int add(int a, int b) {
    return a + b;
}
user:я створив файл unit_tests.cpp потрібно щоб ти тепер Написав юніт тести
  copilot:
#include "math_operations.h"
#include <cassert>
#include <iostream>

void test_add() {
    // Test case 1: Positive numbers
    assert(add(2, 3) == 5);

    // Test case 2: Negative numbers
    assert(add(-2, -3) == -5);

    // Test case 3: Mixed positive and negative numbers
    assert(add(5, -3) == 2);

    // Test case 4: Adding zero
    assert(add(0, 5) == 5);
    assert(add(5, 0) == 5);

    // Test case 5: Large numbers
    assert(add(1000000, 2000000) == 3000000);

    std::cout << "All tests passed!" << std::endl;
}

int main() {
    test_add();
    return 0;
}
user:дивися ти мені робив CMakeLits тепер потрібно щоб ти оновив код додав підтримку Google Test у мій CMakeLists.txt.
  copilot:cmake_minimum_required(VERSION 3.10)

# Set the project name
project(HelloWorld)

# Specify the C++ standard
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED True)

# Add the executable for the main program
add_executable(HelloWorld main.cpp)

# Enable testing
enable_testing()

# Add Google Test
# Download and unpack Google Test at configure time
include(FetchContent)
FetchContent_Declare(
  googletest
  URL https://github.com/google/googletest/archive/refs/tags/release-1.12.1.zip
)
# For Windows: Prevent overriding the parent project's compiler/linker settings
set(gtest_force_shared_crt ON CACHE BOOL "" FORCE)
FetchContent_MakeAvailable(googletest)

# Add the test executable
add_executable(unit_tests unit_tests.cpp math_operations.cpp)

# Link Google Test libraries
target_link_libraries(unit_tests gtest gtest_main)

# Add tests
add_test(NAME UnitTests COMMAND unit_tests)

user:я мав наувазі ти мав Питання ти додав інструкції для створення тестового виконуваного файлу та запуску тестів в CMakeLists.txt. онови код
  cmake_minimum_required(VERSION 3.10)

# Set the project name
project(HelloWorld)

# Specify the C++ standard
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED True)

# Add the executable for the main program
add_executable(HelloWorld main.cpp)

# Enable testing
enable_testing()

# Add Google Test
# Download and unpack Google Test at configure time
include(FetchContent)
FetchContent_Declare(
  googletest
  URL https://github.com/google/googletest/archive/refs/tags/release-1.12.1.zip
)
# For Windows: Prevent overriding the parent project's compiler/linker settings
set(gtest_force_shared_crt ON CACHE BOOL "" FORCE)
FetchContent_MakeAvailable(googletest)

# Add the test executable
add_executable(unit_tests unit_tests.cpp math_operations.cpp)

# Link Google Test libraries
target_link_libraries(unit_tests gtest gtest_main)

# Add tests
add_test(
  NAME UnitTests
  COMMAND unit_tests
)

# Instructions for building and running tests
message(STATUS "To build the tests, run: cmake --build .")
message(STATUS "To execute the tests, run: ctest --output-on-failure")
  
  user:дивися я мав початковий код  хелов ворд тепер допиши і  типу а і б для вункцій і виклик функції
copilot:
#include <iostream>
#include "math_operations.h"

int main() {
    // Змінні для функції add
    int a = 5;
    int b = 10;

    // Виклик функції add
    int result = add(a, b);

    // Виведення результату
    std::cout << "Hello, World!" << std::endl;
    std::cout << "The sum of " << a << " and " << b << " is: " << result << std::endl;

    return 0;
}
